{% extends 'base.html.twig' %}

{% block title %}Chemise{% endblock %}

{% block body %}
    <h1>Chemise</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chemise.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chemise.nom }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ chemise.taille }}</td>
            </tr>
            <tr>
                <th>Couleur</th>
                <td>{{ chemise.couleur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chemise_index') }}">back to list</a>

    <a href="{{ path('app_chemise_edit', {'id': chemise.id}) }}">edit</a>

    {{ include('chemise/_delete_form.html.twig') }}
{% endblock %}
