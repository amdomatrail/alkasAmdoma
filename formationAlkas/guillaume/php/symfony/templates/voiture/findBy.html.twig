{% extends 'base.html.twig' %}

{% block title %}Voiture find by{% endblock %}

{% block body %}
    <h1>Voiture find by : </h1>

    <section class="table">


            {% for voiture in voitures %}
                <p>{{ voiture.nom }}</p>

            {% endfor %}
        <a href="{{ path('app_voiture_findAll') }}">lien vers findAll</a><br>
        <a href="{{ path('app_voiture_find') }}">lien vers find</a><br>
        <a href="{{ path('app_voiture_findBy') }}">lien vers findBy</a><br>
        <a href="{{ path('app_voiture_findOneBy') }}">lien vers findOneBy</a><br>
    </section>


{% endblock %}
