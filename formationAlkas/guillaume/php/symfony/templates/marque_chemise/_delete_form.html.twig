<form method="post" action="{{ path('app_marque_chemise_delete', {'id': marque_chemise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ marque_chemise.id) }}">
    <button class="btn">Delete</button>
</form>
